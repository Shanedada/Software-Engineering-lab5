# Test and coverage dependencies (for CI)
pytest>=7.0
pytest-cov>=4.0
hypothesis>=6.64.0
coverage>=6.5

# Linting / formatting (optional)
flake8>=5.0
black>=24.3.0

# Add other runtime deps below as your project grows
